- hosts: all
  tasks:
    - name: Print hello world
      shell:
        executable: /bin/bash
        cmd: ls -la
    - name: Print src
      shell:
        executable: /bin/bash
        cmd: ls -la src/github.com/SRP-Platform
    - name: Print hello world 2
      shell:
        executable: /bin/bash
        cmd: date